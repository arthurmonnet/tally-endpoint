# Secret token — must match the token you set in the Tally macOS app
TALLY_API_TOKEN=your-secret-token-here

# Vercel KV (auto-populated when you add a KV store in the Vercel dashboard)
KV_REST_API_URL=
KV_REST_API_TOKEN=
